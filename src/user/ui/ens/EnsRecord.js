import React from 'react';

const EnsRecord = ({ name, address }) => (
    <tr>
        <td>
            {name}
        </td>
        <td>
            {address}
        </td>
    </tr>
)

export default EnsRecord